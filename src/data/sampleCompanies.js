/**
 * Initial Lead Dataset (Empty by default to eliminate confusing template/placeholder leads).
 * Live leads are generated on-demand when user runs target discovery searches.
 */
export const INITIAL_COMPANIES = [];
